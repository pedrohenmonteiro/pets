import FormSignUp from "../components/FormSignUp";
import Auth from "../template/Auth";

const SignUp = () => {
  return (
    <Auth title="Sign in">
      <FormSignUp />
    </Auth>
  );
};

export default SignUp;
